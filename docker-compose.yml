version: '2'

services:
        nginx:
                image: nginx:latest
                ports:
                        - 8080:80
                volumes:
                        - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
                        - ./static/:/var/www/html/static/
        web:
                build: ./Web
                #command: stack exec Web-exe
                expose:
                        - 3000
                volumes:
                        - ./static:/usr/src/app/static
